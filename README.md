# pi-questions

A Pi extension for asking structured questions through a small, keyboard-friendly terminal UI.

The project is in the specification phase. The current product definition is in [`docs/spec.md`](docs/spec.md).
